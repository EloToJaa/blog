---
import type { TocItem } from "@utils/generateToC";
import TocElement from "./TocElement.astro";

export type Props = {
  items: TocItem[];
};

const { items } = Astro.props;
---

<ul class="list-none not-prose pl-4">
  {items.map(item => <TocElement item={item} />)}
</ul>
